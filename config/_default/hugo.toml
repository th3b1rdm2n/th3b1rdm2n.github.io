# set `baseurl` to your root domain
# if you set it to "/" share icons won't work properly on production
baseurl = "https://th3b1rdm2n.github.io/" # Include trailing slash
title = "Seeker,  Researcher, and Evolver."  # Edit directly from config/_default/languages.toml # alternatively, uncomment this and remove `title` entry from the aforemention file.
copyright = "Bírd Màn"
# canonifyurls = true

theme = "hugo-clarity"
disqusShortname = ""

DefaultContentLanguage = "en"
# [languages]
# config/_default/languages.toml

# [menus]
# config/_default/menus/menu.xx.toml

[taxonomies]
category = "categories"
tag = "tags"
series = "series"

[outputs]
home = ["HTML", "RSS", "JSON"]

[pagination]
pagerSize = 10